# odd
#include <stdio.h>
void odd(int c)
{
	if(c%2==1)
	{
		printf("the minimum positive integer is %d",c);
	}
}
main()
{
int n,i,c;
printf("Enter number");
scanf("%d",&n);
for(i=0;i<n-1;i++);
{
	if(n/i==c);
	{
		odd(c);
	}
}
}
